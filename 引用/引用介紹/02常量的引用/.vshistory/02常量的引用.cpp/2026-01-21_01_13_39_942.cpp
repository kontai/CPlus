#define _CRT_SECURE_NO_WARNINGS
#include<iostream>

using namespace std;

/*
*2026/01/21
*AUTHOR:kontai
*
*本文件演示C++中常量引用的使用和bool類型的特性
*/

int main() {
	//=============================================================================
	// 第一部分：普通引用與常量引用的區別
	//=============================================================================

	// 普通變數和普通引用：可以修改
	int a = 10;
	int &ref = a;			// ref 綁定到變數 a
	ref = 20;				// 可以通過引用修改變數 a 的值

	//=============================================================================
	// 常量引用的正確用法
	//=============================================================================

	// 錯誤做法：普通引用綁定到常數值（編譯錯誤）
	// int& ref2 = 10;		// ❌ 編譯錯誤！普通引用不能綁定到臨時值或字面量
	
	// 正確做法：常量引用可以綁定到常數值
	const int& ref3 = 10;	// ✓ 正確：常量引用可以綁定到字面量（臨時值）
	// ref3 = 200;			// ❌ 編譯錯誤！常量引用不能修改所引用的值
	
	// 說明：常量引用擴大了引用的適用範圍
	// 1. 可以綁定到臨時值或常數字面量
	// 2. 保護被引用的對象不被修改（增強型安全性）
	// 3. 常用於函數參數中，表示函數不會修改傳入的參數

	//=============================================================================
	// 第二部分：bool 類型和條件判斷
	//=============================================================================

	// bool 類型變數初始化
	bool is = 0;			// is 初始化為 false（0表示假）
	
	// 重要提示：bool 類型的轉換規則
	// - 0 表示 false（假）
	// - 非0值（包括負數、任何非零整數）都表示 true（真）
	
	if (is)
	{
		cout << "真" << endl;	// 當 is 為 true 時執行
	}
	else
	{
		cout << "假" << endl;	// 當 is 為 false 時執行，此時會輸出"假"
	}

	return EXIT_SUCCESS;
}